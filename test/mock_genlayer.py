"""
Compatibility shim — no longer used.

Tests now use the official gltest `direct_vm` / `direct_deploy` fixtures
which load the real GenVM SDK (genlayer-py-std) downloaded from GitHub.

This file is kept as an empty placeholder to avoid breaking any imports
that may still reference it.
"""
